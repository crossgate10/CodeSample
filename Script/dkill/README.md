#### In Ubuntu
    pkill debug

#### In Windows Cmd (use .bat) / Git Bash (use .sh)
    dkill

P.S.

1.put the batch/sh file to go bin directory and add the position to enviroment, then it could be used in cmd.
